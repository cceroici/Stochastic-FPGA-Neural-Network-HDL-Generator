#ifndef BOps
#define BOps

#include <string>

using namespace std;

double roundval(double in);

string Dec2Bin(int n);

string PadZeros(string A,int L);

void PrintMatrix(int** A, int w, int h);

int SumVector(int* A, int size);

#endif
